config:
  info: gateway-dev


spring:
  servlet:
    # 设置文件接收
    multipart:
      enabled: true
      max-file-size: 100MB
      max-request-size: 100MB

# 日志打印级别
logging:
  file: target/app.log
  level:
    root: WARN
    com.demo.springcloud: DEBUG